# Navigation
alias ..="cd .."

# Time management
alias week='date +%V'
alias timer='echo "Timer started. Stop with CTRL-D." && date && time cat && date'

# Python
alias python=python3
alias pip=pip3

# Pipenv
alias pv=pipenv
alias pvi="pipenv install"
alias pvu="pipenv uninstall"